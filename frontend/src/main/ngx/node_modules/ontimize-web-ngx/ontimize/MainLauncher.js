import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';
export function ontimizeBootstrap(appModule, config) {
    var promise = platformBrowserDynamic().bootstrapModule(appModule);
    promise.then(function (moduleRef) {
        console.log('Bootstrap Successful');
        return ontimizePostBootstrap(moduleRef);
    }).catch(function (err) {
        console.error(err.message);
    });
    return promise;
}
export function ontimizePostBootstrap(ngModuleRef) {
    var loader = document && document.getElementById('loader-wrapper');
    if (loader && loader.parentNode) {
        loader.parentNode.removeChild(loader);
    }
    return ngModuleRef;
}
//# sourceMappingURL=MainLauncher.js.map