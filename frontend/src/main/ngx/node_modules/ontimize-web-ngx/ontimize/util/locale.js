export var REG_LANGUAGE = /^([a-z]{2})[-|_]([A-Z]{2})$/;
var LocaleCode = (function () {
    function LocaleCode() {
    }
    LocaleCode.getLanguageCode = function (code) {
        var match = code.match(REG_LANGUAGE);
        var result = code.toLowerCase();
        if (match && match.length > 1) {
            result = match[1].toLowerCase();
        }
        return result;
    };
    LocaleCode.getCountryCode = function (code) {
        var match = code.match(REG_LANGUAGE);
        var result = code.toLowerCase();
        if (match && match.length > 2) {
            result = match[2].toLowerCase();
        }
        else {
            if (result === 'pt') {
                result = 'br';
            }
        }
        return result;
    };
    return LocaleCode;
}());
export default LocaleCode;
//# sourceMappingURL=locale.js.map