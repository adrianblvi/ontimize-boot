import { Util } from './util';
describe('Util => parseArray', function () {
    var empty = '';
    var arrayData = 'one;two;three;three;;';
    it('empty array', function () {
        expect(Util.parseArray(empty)).toEqual([]);
    });
    it('array with repeated elements', function () {
        expect(Util.parseArray(arrayData)).toEqual(['one', 'two', 'three', 'three', '', '']);
    });
    it('array without repeated elements', function () {
        expect(Util.parseArray(arrayData, true)).toEqual(['one', 'two', 'three', '']);
    });
});
//# sourceMappingURL=util.spec.js.map