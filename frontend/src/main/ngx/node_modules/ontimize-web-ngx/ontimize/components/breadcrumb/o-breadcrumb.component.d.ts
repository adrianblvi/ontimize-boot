import { AfterViewInit, Injector, OnDestroy, OnInit } from '@angular/core';
import { ActivatedRouteSnapshot, Router } from '@angular/router';
import { Subscription } from 'rxjs';
import { NavigationService, OFormComponent, ONavigationItem } from '../../../index';
export declare const DEFAULT_INPUTS_O_BREADCRUMB: string[];
export declare class OBreadcrumbComponent implements AfterViewInit, OnDestroy, OnInit {
    protected injector: Injector;
    labelColumns: string;
    separator: string;
    breadcrumbs: Array<ONavigationItem>;
    protected router: Router;
    protected _formRef: OFormComponent;
    protected labelColsArray: Array<string>;
    protected navigationService: NavigationService;
    protected onDataLoadedSubscription: Subscription;
    protected navigationServiceSubscription: Subscription;
    protected _displayTextloaded: boolean;
    constructor(injector: Injector);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    showBreadcrumbItem(item: ONavigationItem): boolean;
    isNotInsideFormLayoutManager(item: ONavigationItem, index: number): boolean;
    protected isTerminal(route: ActivatedRouteSnapshot): boolean;
    ngOnDestroy(): void;
    onRouteClick(route: any): void;
    displayTextloaded: boolean;
}
export declare class OBreadcrumbModule {
}
