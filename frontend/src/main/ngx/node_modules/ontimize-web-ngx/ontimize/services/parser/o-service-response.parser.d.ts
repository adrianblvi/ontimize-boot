import { Injector } from '@angular/core';
import { Subscriber } from 'rxjs';
import { IAuthService } from '../../utils';
export declare class OntimizeServiceResponseParser {
    protected injector: Injector;
    constructor(injector: Injector);
    parseSuccessfulResponse(resp: any, subscriber: Subscriber<any>, service: IAuthService): void;
    parseUnsuccessfulResponse(error: any, subscriber: Subscriber<any>, service: IAuthService): void;
}
