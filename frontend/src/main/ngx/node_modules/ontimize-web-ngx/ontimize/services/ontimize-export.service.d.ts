import { HttpClient } from '@angular/common/http';
import { Injector } from '@angular/core';
import { Observable } from 'rxjs';
import { AppConfig, Config } from '../config/app-config';
export declare const EXPORT_PATH_DEFAULT: string;
export declare const DOWNLOAD_PATH_DEFAULT: string;
export declare class OExportExtension {
    static Excel: string;
    static HTML: string;
    static PDF: string;
}
export declare class OntimizeExportService {
    protected injector: Injector;
    EXPORT_PATH_DEFAULT: string;
    DOWNLOAD_PATH_DEFAULT: string;
    exportPath: string;
    downloadPath: string;
    protected httpClient: HttpClient;
    protected _sessionid: string;
    protected _urlBase: string;
    protected _appConfig: Config;
    protected _config: AppConfig;
    constructor(injector: Injector);
    getDefaultServiceConfiguration(serviceName?: string): Object;
    configureService(config: any): void;
    urlBase: string;
    exportData(data: any, format: string): Observable<any>;
    downloadFile(fileId: string, fileExtension: string): Observable<any>;
    protected redirectLogin(sessionExpired?: boolean): void;
}
