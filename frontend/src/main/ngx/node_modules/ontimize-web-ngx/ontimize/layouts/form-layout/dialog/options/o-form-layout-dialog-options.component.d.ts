import { DialogPosition } from '@angular/material';
import { OFormLayoutManagerComponent } from '../../o-form-layout-manager.component';
export declare const DEFAULT_INPUTS_O_FORM_LAYOUT_DIALOG_OPTIONS: string[];
export declare class OFormLayoutDialogOptionsComponent {
    protected oFormLayoutManager: OFormLayoutManagerComponent;
    constructor(oFormLayoutManager: OFormLayoutManagerComponent);
    width: string;
    minWidth: number | string;
    maxWidth: number | string;
    height: string;
    minHeight: number | string;
    maxHeight: number | string;
    class: string | string[];
    position: DialogPosition;
    backdropClass: string;
    closeOnNavigation: boolean;
    disableClose: boolean;
}
